# Assets

Placeholder images are referenced via:
- Inline base64 in HTML (intentionally oversized)
- External placeholder URLs (unoptimized, no dimensions)

Replace with optimized WebP/AVIF during refactoring.
